services:

  backend:
    build:
      context: ./backend
    container_name: demo-backend
    ports:
      - "5000:5000"

  frontend:
    build:
      context: ./frontend
    container_name: demo-frontend
    ports:
      - "8080:80"
    depends_on:
      - backend